# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore, with the addition of
# "#!include" directives (which insert the entries of the given .gitignore-style
# file at that point).

# Exclude TypeScript source files (only deploy compiled JS)
src/

# Exclude TypeScript config
tsconfig.json

# Exclude linter and formatter configs
.eslintrc.js
.prettierrc

# Exclude development dependencies lock files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Exclude node_modules (Firebase will install production dependencies)
node_modules/

# Exclude test files
**/*.test.ts
**/*.spec.ts
**/*.test.js
**/*.spec.js
test/

# Exclude documentation
*.md
!README.md

# Exclude backup files
*.backup

# Exclude git files
.git/
.gitignore

# Exclude IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Exclude build artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exclude macOS files
.DS_Store
